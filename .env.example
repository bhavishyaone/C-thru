# Required — Postgres connection string
DATABASE_URL=postgres://postgres:postgres@localhost:5432/cthru

# Required — public key shipped in the browser snippet (safe to expose)
CTHRU_WRITE_KEY=

# Required — secret key used only in your backend server SDK (never expose)
CTHRU_SERVER_KEY=
